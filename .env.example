# ── Mestre das Vendas — Variáveis de Ambiente ──
# Renomeie para .env antes de rodar localmente

# Segredo para assinar os tokens JWT (coloque qualquer texto longo e aleatório)
JWT_SECRET=mestre_das_vendas_segredo_super_secreto_2025

# Chave da API Anthropic
ANTHROPIC_API_KEY=sk-ant-SUA_CHAVE_AQUI

# Porta (Railway define automaticamente)
PORT=3000
